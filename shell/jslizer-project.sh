alias jslizer_build="cd $SYSTEM_ROOT_GIT_REPO_FOLDER/jslizer/ && npm install && npm_build"
alias jslizer_publish="cd $SYSTEM_ROOT_GIT_REPO_FOLDER/jslizer/ && npm_publish"
alias jslizer_demo_build="cd $SYSTEM_ROOT_GIT_REPO_FOLDER/jslizer-demo/ && npm install && npm update"
